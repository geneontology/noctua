# Noctua Metadata

Upstream source metadata for Noctua and related projects.

## Files

- `allowed-references.yaml` — Allowed namespaces for references, defined as a [LinkML](https://linkml.io/) enum.
- `with-from-allowed-namespaces.yaml` — Allowed namespaces for with/from fields, defined as a [LinkML](https://linkml.io/) enum.

## Validation

Requires [LinkML](https://linkml.io/linkml/intro/install.html) (`pip install linkml`).

```bash
make validate
```
