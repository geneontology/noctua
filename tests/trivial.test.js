var { describe, it } = require('node:test')
var assert = require('chai').assert;

describe.skip('placeholder', function(){
  it('TODO: remove this test and add real tests', function(){
    assert.equal(1, 1);
  });
});
